{
  "routes": [
    {
      "method": "GET",
      "path": "/songs",
      "handler": "songs.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/songs/count",
      "handler": "songs.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/songs/:id",
      "handler": "songs.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/songs",
      "handler": "songs.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/songs/:id",
      "handler": "songs.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/songs/:id",
      "handler": "songs.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
